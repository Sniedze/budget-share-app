# Copy to `.env.local` (gitignored) for local `npm run dev` / `npm run server`.
# Overrides values from `.env` without changing production-oriented settings there.
#
# NODE_ENV=development
# ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
